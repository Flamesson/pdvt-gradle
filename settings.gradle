pluginManagement {
    repositories {
        maven {
            url System.getenv("GRADLE_PLUGINS")
            allowInsecureProtocol true
        }

        gradlePluginPortal()

        mavenLocal()
    }
}

rootProject.name = 'pdvt'
